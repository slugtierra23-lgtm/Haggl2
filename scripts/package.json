{
  "name": "haggl-scripts",
  "version": "1.0.0",
  "description": "Standalone utility scripts for haggl (bots, data backfills, etc.)",
  "private": true,
  "scripts": {
    "daian": "ts-node --transpile-only daian-bot.ts",
    "start": "npm run daian"
  },
  "dependencies": {
    "socket.io-client": "^4.7.5"
  },
  "devDependencies": {
    "@types/node": "^20.12.7",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.5"
  }
}
